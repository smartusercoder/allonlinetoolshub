# Cloudflare Pages Configuration for All Online Tools Hub
# For Pages projects, most configuration is done via the Cloudflare dashboard
# Build command: npm run build (defined in package.json)
# Build output directory: . (root directory)
# Custom headers: _headers file
# Custom redirects: _redirects file

name = "allonlinetoolshub"
compatibility_date = "2025-01-03"
pages_build_output_dir = "."
